name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install Dependencies
        run: npm install

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no -i /path/to/gokul.pem gokul@52.22.48.83 "cd /c/Users/Nagagogulan/Downloads/node-appp && git pull origin main && npm install && pm2 restart node-appp"

        env:
          NODE_ENV: production
